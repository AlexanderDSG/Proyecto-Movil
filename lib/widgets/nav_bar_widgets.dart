import "package:flutter/material.dart";

class nav_bar_widgets extends StatelessWidget {
  const nav_bar_widgets({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
} 